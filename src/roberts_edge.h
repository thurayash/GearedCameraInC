#ifndef ROBERTS_EDGE
#define ROBERTS_EDGE

#include <SDL/SDL.h>
#include <SDL/SDL_image.h>
#include <math.h>

SDL_Surface *to_rob(SDL_Surface *image);

SDL_Surface *first_rob(SDL_Surface *image);
#endif
